<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wekid">
    <!-- 인터넷 퍼미션 추가 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ParentsHomeActivity" />
        <activity android:name=".TeacherHomeActivity" />
        <activity android:name=".PrincipalHomeActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".DeleteMemberActivity"/>
        <activity android:name=".UpdateMemberActivity"/>
        <activity android:name=".InsertTeacherActivity"/>
        <activity android:name=".InsertParentActivity"/>
        <activity android:name=".InsertPrincipalActivity"/>
        <activity android:name=".PostManageActivity" />
        <activity android:name=".PostMainActivity" />
        <activity android:name=".TeacherManageActivity" />
        <activity android:name=".InsertKinderActivity" />
        <activity android:name=".KinderActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppThemeNoTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>